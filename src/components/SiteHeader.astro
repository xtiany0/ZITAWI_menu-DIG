---
import { ui, type Lang } from "../i18n/ui";
import type { CATEGORIES } from "../content.config";

interface Props {
  lang: Lang;
  categories: (typeof CATEGORIES)[number][];
}

const { lang, categories } = Astro.props;
const t = ui[lang];
---

<header class="sticky top-0 z-30">
  <div class="relative border-b-[5px] border-ink bg-terracotta px-5 pt-[15px] pb-[13px] text-center">
    <a href={`/${lang}/`} class="logo block font-logo text-[34px] leading-none font-black tracking-[.11em]">
      ZITAWI
    </a>
    <p class="mt-1 font-narrow text-[12px] font-semibold tracking-[.2em] text-blush uppercase">
      {t.tagline}
    </p>

    <div
      class="absolute top-[14px] right-4 flex rounded-full border-2 border-ink bg-cream p-[2px] shadow-[2px_2px_0_#141210]"
    >
      <span class="rounded-full bg-ink px-[11px] py-[5px] font-narrow text-[13px] font-bold text-band">
        {lang === "fr" ? "FR" : "EN"}
      </span>
      <a
        href={t.otherLangHref}
        hreflang={lang === "fr" ? "en" : "fr"}
        class="rounded-full px-[11px] py-[5px] font-narrow text-[13px] font-bold text-ink transition-colors hover:bg-band"
      >
        {t.otherLang}
      </a>
    </div>
  </div>

  <div class="flex flex-col gap-[9px] bg-ink px-5 pt-[11px] pb-3">
    <label class="flex items-center gap-2 rounded-md bg-band px-3 py-[9px]">
      <span class="text-[15px] text-[#6b5510]" aria-hidden="true">⌕</span>
      <input
        type="search"
        data-search
        aria-label={t.searchPlaceholder}
        placeholder={t.searchPlaceholder}
        class="w-full bg-transparent font-body text-[14px] font-semibold text-ink placeholder:text-[#6b5510]/70 focus:outline-none"
      />
    </label>

    <p class="text-center font-narrow text-[13px] font-semibold">
      <span class="text-band">{t.hours}</span>
      <span
        class="mx-2 inline-block h-1 w-1 rounded-full bg-terracotta align-middle"
        aria-hidden="true"></span>
      <span class="text-sand">{t.service}</span>
    </p>
  </div>

  <div class="rail flex gap-[7px] overflow-x-auto border-b-[3px] border-ink bg-band px-5 py-[9px]">
    <button
      type="button"
      data-tab="all"
      aria-pressed="true"
      class="shrink-0 rounded-full border-2 border-ink bg-white/55 px-3 py-1.5 font-tab text-[12.5px] font-extrabold text-ink aria-pressed:bg-ink aria-pressed:text-band"
    >
      {t.allCategories}
    </button>
    {
      categories.map((category) => (
        <button
          type="button"
          data-tab={category}
          aria-pressed="false"
          class="shrink-0 rounded-full border-2 border-ink bg-white/55 px-3 py-1.5 font-tab text-[12.5px] font-extrabold text-ink aria-pressed:bg-ink aria-pressed:text-band"
        >
          {t.categories[category]}
        </button>
      ))
    }
  </div>
</header>
